#variables store values for later use
x = 7
#variable is x and value is 7
#"x=7" is an assignment statement that reads x is assigned the value 7
#assignment symbol "="  give the variable a value
y = 7
total = x+y
# "+" is  an addition operator with also being a binary since it require two operands "x" and "7"
#The right side of the = symbol always executes first, then the result is assigned to the variable on the symbol’s left side.

#A variable name, such as x, is an identifier. Each identifier may consist of letters, digits and underscores (_) but may not begin with a digit. Python is case sensitive, so number andNumber are different identifiers because one begins with a lowercase letter and the other begins with an uppercase letter

g = type(x)
# the type function determines the data type of the variable
#A function performs a task when you call it by writing its name, followed by parentheses, (). The parentheses contain the function’s argument—the data that the type function needs to perform its task.
